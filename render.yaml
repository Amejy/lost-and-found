services:
  - type: web
    name: lost-and-found
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --listen=0.0.0.0:$PORT run:app
    autoDeploy: true
