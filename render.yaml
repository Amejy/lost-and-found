services:
  - type: web
    name: lost-and-found
    env: python
    plan: free
    healthCheckPath: /healthz
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c 'exec waitress-serve --host=0.0.0.0 --port=${PORT:-8080} run:app'
    autoDeploy: true
